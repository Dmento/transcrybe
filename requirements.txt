# Pinned for reproducible Streamlit Cloud builds.
#
# IMPORTANT: deploy on Python 3.12 (Streamlit Cloud → Manage app → Settings →
# Python version). The WebRTC stack (streamlit-webrtc, av/PyAV, aiortc) is not
# reliable on Python 3.14 — it crashes in streamlit-webrtc's shutdown handler
# because it can't hook Streamlit's session internals on that runtime.
streamlit==1.58.0
streamlit-webrtc==0.72.1
# av must stay <17: streamlit-webrtc 0.72.1 -> aiortc 1.14.0 requires av<17.
av==16.1.0
azure-cognitiveservices-speech
python-dotenv
imageio-ffmpeg
requests
